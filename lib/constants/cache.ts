/** Cache time-to-live in milliseconds (1 hour). */
export const CACHE_TTL_MS = 3_600_000;

/** Interval in milliseconds between automatic cleanup of expired cache entries (10 minutes). */
export const CACHE_CLEANUP_INTERVAL_MS = 600_000;